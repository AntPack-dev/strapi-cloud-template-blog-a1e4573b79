'use strict';

/**
 * subscription-section router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::subscription-section.subscription-section');
