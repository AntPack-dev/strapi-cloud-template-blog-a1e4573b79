'use strict';

/**
 * subscription-section service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subscription-section.subscription-section');
